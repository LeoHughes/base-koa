extends layout

block content
  h1= title
  p Welcome to #{title}
  img(src="/api/codeImg")
  script(src="https://cdn.bootcss.com/jquery/3.2.1/jquery.min.js")
  script.
    $.post('/api/login',{'userName':'hughes','passWord':123}).done(function(res){
      console.log(res)
    })